# API overview

Base URL: `https://api.cheddaboards.com`

All requests and responses are JSON. Authenticate games with an API key and players with a session token.

TODO: list of resources, versioning note, rate limit (universal 2 seconds per player between submits), CORS.
